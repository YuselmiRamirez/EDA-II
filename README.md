# Descripcion del codigo
//Lineas 1, 2
definimos el doble para realizar la multiplicacion de cada elemento del arreglo por dos
//Lineas 4, 7
se define el maximo que, de no existir se muestra un mensaje de NONE, o se regresa el numero mas grande 
//Lineas 9, 15
se define el promedio y cuando no existe se muestra el resultado como 0, en caso contrario total empieza en 0 y se crea un loop donde el total es la suma de cada elemento que luego se divide entre len(arr) que es el conteo de elementos total
//Lineas 17, 23
definimos el main y las listas, en este caso 4 donde se presentan casos de numeros pequenos, grandes, negativos y un arreglo vacio
//Lineas 24, 29
se realiza la impresion de datos, agregando el ultimo print vacio para mostrar un espacio entre impresiones
//Lineas 31, 32
if __name__ == "__main__": main() es utilizado dentro de python para ejecutar el main() solo cuando este corriendo adecuadamente o para que solo el script sea ejecutado pero no cuando se trate de un modulo dentro de otro script
